# #!/usr/bin/env bash

tmux send-keys 'nvim' Enter
tmux new-window -c "#{session_path}"
tmux send-keys 'docker-compose up --build -d; docker-compose down frontend; npm run dev' Enter
tmux select-window -p


