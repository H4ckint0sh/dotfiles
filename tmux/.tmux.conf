# catppuccin palette colors
bg="#222436"
bg_dark="#1e2030"
bg_highlight="#2f334d"
blue="#82aaff"
blue0="#3e68d7"
blue1="#65bcff"
blue2="#0db9d7"
blue5="#89ddff"
blue6="#b4f9f8"
blue7="#394b70"
comment="#636da6"
cyan="#86e1fc"
dark3="#545c7e"
dark5="#737aa2"
fg="#c8d3f5"
fg_dark="#828bb8"
fg_gutter="#3b4261"
green="#c3e88d"
green1="#4fd6be"
green2="#41a6b5"
magenta="#c099ff"
magenta2="#ff007c"
orange="#ff966c"
purple="#fca7ea"
red="#ff757f"
red1="#c53b53"
teal="#4fd6be"
terminal_black="#444a73"
yellow="#ffc777"


# set the terminals
set-option -g detach-on-destroy off
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",*256col*:Tc"
set -s set-clipboard external
set -as terminal-features ',tmux-256color:clipboard'
set -ga terminal-overrides '*:Ss=\E[%p1%d q:Se=\E[ q' # this is for the cursor shape
set -g mouse on

# Reload config
bind r source-file ~/.tmux.conf \; \
display-message "Config Reloaded."

# remove confirm step when killing a window or pane
bind-key & kill-window
bind-key x kill-pane

# Reduce delay
set -s escape-time 1

#env
setenv -g SHOW_DIRECTORY_NAME 1

#statusbar
set -g status-position top

# Window and pane index form 1
set -g base-index 1
set -g pane-base-index 1
set -gw pane-active-border-style 'bg=default'
set -gw pane-border-style ""
set -gw pane-active-border-style ""
set -gw popup-border-style "fg=$fg"
set -gw popup-border-lines single

# Copy mode
setw -g mode-keys vi
bind-key -T copy-mode-vi v send-keys -X begin-selection
# bind y key in copy mode to select and copy to system clipboard
bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "pbcopy"
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "pbcopy"

# Keyboard shortcut bindings
bind C-b last-window
bind C-h resize-pane -L 1
bind C-j resize-pane -D 1
bind C-k resize-pane -U 1
bind C-l resize-pane -R 1
bind C-n next-window
bind C-p previous-window
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind a run-shell 'tmux popup -d "#{pane_current_path}" -xC -yC -w60% -h60% -E al'
bind-key s run-shell -b tmux-switch-pane.sh

# Sesh
# bind-key "S" run-shell "s"
bind-key "K" run-shell "sesh connect \"$(
	sesh list -i | fzf-tmux -p 55%,60% \
		--no-sort --border-label ' sesh ' --prompt '⚡  ' \
		--header '  ^a all ^t tmux ^x zoxide ^g config ^d tmux kill ^f find' \
		--bind 'tab:down,btab:up' \
		--bind 'ctrl-a:change-prompt(⚡  )+reload(sesh list -i)' \
		--bind 'ctrl-t:change-prompt(🪟  )+reload(sesh list -it)' \
		--bind 'ctrl-g:change-prompt(⚙️  )+reload(sesh list -ic)' \
		--bind 'ctrl-x:change-prompt(📁  )+reload(sesh list -iz)' \
		--bind 'ctrl-f:change-prompt(🔎  )+reload(fd -H -d 2 -t d -E .Trash . ~)' \
		--bind 'ctrl-d:execute(tmux kill-session -t {})+change-prompt(⚡  )+reload(sesh list)'
)\""

# bind-key "Z" display-popup -E "sesh connect \$(sesh list | zf --height 24)"

# Tokyonight 
set -Fg 'status-format[1]' '#{status-format[0]}'
set -g 'status-format[1]' ''
set -g status 2

set -g mode-style "fg=#7aa2f7,bg=#3b4261"
set -g message-style "fg=#7aa2f7,bg=#3b4261"
set -g message-command-style "fg=#7aa2f7,bg=#3b4261"
set -g pane-border-style "fg=#3b4261"
set -g pane-active-border-style "fg=#7aa2f7"
set -g status-justify "left"
set -g status-style "fg=#7aa2f7,bg=#16161e"
set -g status-left-length "100"
set -g status-right-length "100"
set -g status-left-style NONE
set -g status-right-style NONE

set -g status-left "#[fg=$fg,bg=$bg_highlight,bold] ░▒▓#[fg=$bg,bg=$fg]#S#[fg=$fg,bg=$bg_dark]#[bg=$bg_dark]#(gitmux -cfg $HOME/gitmux.conf '#{pane_current_path}')#[fg=$bg_dark,nobold,nounderscore,noitalics]"
set -g status-right "#[fg=#7aa2f7,bg=#16161e] #{prefix_highlight}#[fg=$bg_highlight,bg=$bg,nobold,nounderscore,noitalics]#[fg=$blue,bg=$bg_highlight,bold] #(~/.tmux/plugins/tmux-mem-cpu-load/tmux-mem-cpu-load --interval 2) "

setw -g window-status-activity-style "underscore,fg=#a9b1d6,bg=#16161e"
setw -g window-status-separator ""
setw -g window-status-style "NONE,fg=#a9b1d6,bg=#16161e"
setw -g window-status-format "#[fg=#16161e,bg=$bg_highlight,nobold,nounderscore,noitalics]#[bg=$bg_dark,fg=$comment] #I #W #F #[fg=#16161e,bg=#16161e,nobold,nounderscore,noitalics]"
setw -g window-status-current-format "#[fg=#16161e,bg=#3b4261,nobold,nounderscore,noitalics]#[fg=$orange,bg=#3b4261,bold] #I #W #F #[fg=#3b4261,bg=#16161e,nobold,nounderscore,noitalics]"

# tmux-plugins/tmux-prefix-highlight support
set -g @prefix_highlight_output_prefix "#[fg=#e0af68]#[bg=#16161e]#[fg=#16161e]#[bg=#e0af68]"
set -g @prefix_highlight_output_suffix ""

# Automatic rename
set-option -g automatic-rename-format '#{pane_current_command}'

# messages
set -g message-style "fg=$fg,bg=default,bold"

# FZF
set -g @fzf-url-fzf-options '-p 60%,30% --prompt="   " --border-label=" Open URL "'
set -g @t-fzf-prompt '  '
set -g @t-bind 'T'
set -g @fzf-url-history-limit '2000'

# restore neovim sessions, used with tmux-resurrect
set -g @resurrect-strategy-nvim 'session'

# restore panes contents
set -g @resurrect-capture-pane-contents 'on'

# enable auto restore on reboot
set -g @continuum-restore 'off'

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'joshmedeski/tmux-fzf-url'
set -g @plugin 'thewtex/tmux-mem-cpu-load'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

# Install `tpm` if needed.
if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"

# Run tpm
run '~/.tmux/plugins/tpm/tpm'
